# Create Claude Skills

# run


Default
```bash
python3 -m app.cli https://defold.com/manuals
```

Delete skill
```bash
python3 -m app.cli --delete https://defold.com/manuals
```

Use LLM
```bash
python3 -m app.cli --clean-with-llm --use-batch https://defold.com/manuals
```
